# Copy to .env to override defaults. No secrets required: the test suite
# registers a fresh, random throwaway user via the API on every run.
BASE_URL=https://conduit.bondaracademy.com
API_BASE_URL=https://conduit-api.bondaracademy.com/api/
